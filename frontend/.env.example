# API Configuration
# Local development with Vite proxy uses /api
# Docker Compose build also injects /api and Nginx proxies it to backend
# Override this with a full URL only when you intentionally call a remote backend
VITE_API_BASE_URL=/api
